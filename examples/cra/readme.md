# CRA Example

Minimal demo of the spinners with create-react-app using ESM bundle.

```sh
# Install the deps
npm i

# Start the server
npm start

# Build the bundle
npm run build

# Serve local build
npm run serve
```
